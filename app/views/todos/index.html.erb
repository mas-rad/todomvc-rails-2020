<ul class="todo-list">
  <li>
    <div class="view">
      <label>Make me dynamic</label>
      <% @todos.each do |todo| %>
        <tr>
          <td><%= todo.title %></td>
          <td><%= todo.completed %></td>
        </tr>
      <% end %>      
    </div>
  </li>
</ul>
